import 'package:flutter/material.dart';

class HitungLuas extends StatelessWidget{
  const HitungLuas({super.key});

  @override
  Widget build(BuildContext context){
    return Scaffold(
      appBar: AppBar(
        title: Text("Hitung Luas", style: TextStyle(color: Colors.white)),
      ),
      body: Container(

      ),
    );

  }
}